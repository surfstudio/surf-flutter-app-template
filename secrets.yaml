# File with secrets. Do not push to the repository. Add to .gitignore after project creation. Used for local runs.
# Firebase authorization token.
firebase_token: ''
# Get it along with the .p8 file from the Apple Store Connect.
testflight_key_id: ''
# Get it along with the .p8 file from the Apple Store Connect.
testflight_issuer_id: ''
