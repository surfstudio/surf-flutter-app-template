import 'package:push_notification/push_notification.dart';

/// Message service.
/// Implementation is required depending on the service you have
/// chosen to send push notifications.
class MessagingService extends BaseMessagingService {
  @override
  void initNotification(HandleMessageFunction handleMessage) {
    // TODO(dev): implement initNotification
  }
}
